[project]
name = "rttpc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "oskarwsson", email = "oskar.wilhelmsson00@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.3",
    "pillow>=12.3.0",
    "torchvision>=0.29.0",
    "transformers[torch]>=5.17.0",
]

[project.scripts]
rttpc = "rttpc:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
